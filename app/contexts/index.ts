const CONTEXT_MODULE_DETAILS =
    'Applications react-context apis are exported through this global file.';
export default CONTEXT_MODULE_DETAILS;

import {ThemeProvider, useTheme} from '../themes';
export {ThemeProvider, useTheme};
